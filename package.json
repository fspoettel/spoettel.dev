{
  "name": "@spoetteldev/workspace",
  "private": true,
  "scripts": {
    "format": "(cd api && yarn format) && (cd www && yarn format)",
    "lint": "(cd api && yarn lint) && (cd www && yarn lint)",
    "dev": "(cd api && yarn dev) & (cd www && yarn dev)"
  }
}
